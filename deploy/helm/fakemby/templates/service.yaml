apiVersion: v1
kind: Service
metadata:
  name: {{ include "fakemby.fullname" . }}
  labels:
    {{- include "fakemby.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "fakemby.selectorLabels" . | nindent 4 }}
